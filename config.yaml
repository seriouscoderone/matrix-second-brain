# config.yaml — written by the !setup wizard
# Contains non-secret preferences only (no credentials).
# This file is managed by the bot. Do not edit by hand while the bot is running.

space:
  id: ""
  name: ""

rooms:
  digest: ""
  inbox: {}  # map of username → room_id

users: []  # list of Matrix IDs with access

# Cron schedules (standard cron syntax)
cron:
  daily_digest: "0 8 * * *"     # 8:00 AM every day
  weekly_review: "0 9 * * 1"    # 9:00 AM every Monday
  enrichment: "0 */6 * * *"     # every 6 hours
